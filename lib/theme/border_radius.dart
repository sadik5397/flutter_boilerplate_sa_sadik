import 'package:flutter/material.dart';

import '../api/constant.dart';

class ThemeBorderRadius {
  static double value = Constant.unitMultiplier;

  //region all
  static BorderRadius r1 = BorderRadius.circular((value * 1));
  static BorderRadius r2 = BorderRadius.circular((value * 2));
  static BorderRadius r3 = BorderRadius.circular((value * 3));
  static BorderRadius r4 = BorderRadius.circular((value * 4));
  static BorderRadius r5 = BorderRadius.circular((value * 5));
  static BorderRadius r6 = BorderRadius.circular((value * 6));
  //endregion
}
